from setuptools import setup, find_packages

setup(
    name="src",
    description="It's mlops project",
    version="0.0.1",
    author="aakanshi",
    packages=find_packages(),
    license="MIT",
)